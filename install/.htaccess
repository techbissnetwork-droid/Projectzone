# The wizard itself must stay reachable, but its data files never are.
<FilesMatch "\.(sql|md|json|log|txt)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
