<?php
/**
 * Edit these four values to match the MySQL database you created in
 * phpMyAdmin (or your host's database panel), then import schema.sql
 * into that same database before loading the site.
 */
define('DB_HOST', 'localhost');
define('DB_NAME', 'techbiss');
define('DB_USER', 'techbiss_user');
define('DB_PASS', 'change-this-password');

// Set to true only while diagnosing a fresh install; leave false in production.
define('APP_DEBUG', false);
